import React from "react";

// Goal is to have input fields for email + password
// buttons for login, register, login w/ google account

const Login = () => {
  return <h1 className="text-4xl text-blue-500">Login</h1>;
};

export default Login;
